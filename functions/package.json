{
  "name": "functions",
  "version": "1.0.0",
  "description": "",
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "prebuild": "node -e \"const fs = require('fs'); const code = fs.readFileSync('src/index.ts', 'utf8'); if (!code.includes('TEST_PHONE_ALLOWLIST') || !code.includes('isLoadTestMode')) { console.error('ERROR: Load-test bypass logic is missing from src/index.ts!'); process.exit(1); }\"",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.2.5"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3"
  }
}
